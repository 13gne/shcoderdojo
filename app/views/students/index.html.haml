%p#notice= notice
%h1 Listing Students
%table
  %thead
    %tr
      %th User
      %th Name
      %th Email
      %th{:colspan => "3"}
  %tbody
    - @students.each do |student|
      %tr
        %td= student.user_id
        %td= student.name

%br/
= link_to 'New Student', new_student_path
